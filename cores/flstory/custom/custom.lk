MEMORY {
    ROM (rx) : ORIGIN = 0x0000, LENGTH = 0x4000
    RAM (rw) : ORIGIN = 0xC000, LENGTH = 0x0800
}

SECTIONS {
    .text : { *(.text) } > ROM
    .rodata : { *(.rodata) } > ROM
    .data : { *(.data) } > RAM
    .bss : { *(.bss) } > RAM
}
