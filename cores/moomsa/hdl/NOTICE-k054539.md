# K054539 implementation provenance

`jtmoomsa_k054539_full.v` and the accompanying `voltab.hex`, `pantab.hex`,
and `rram_zero.hex` data files were adapted from the local Violent Storm core
at `D:\Arcade\AI\aCORES\Violent storm\cores\viostorm\hdl` on 2026-08-26.

The imported RTL carries its original GPL notice. Preserve GPLv3 licensing
and the source notice when redistributing this component. The adapter
`jtmoomsa_054539.v` is Moo Mesa-specific glue and keeps the existing Moo
sound/SDRAM interface. No code or data was taken from
`github.com/jlrh/konami-fpga`.
