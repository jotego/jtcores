# PSAC accepted-ROM handoff

Checks that the PSAC presents an accepted ROM pixel combinationally to its
line-buffer writer. This prevents a one-virtual-pixel delay from associating a
projected pixel with the previous source coordinate.
