# GunCon mux unit test

Checks that the MiSTer GunCon selector registers the standard JTFRAME gun and
joystick outputs, preserves board input outside GunCon mode, and reports
off-screen GunCon coordinates as out of range.
